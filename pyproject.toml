[project]
name = "agentic-coding-workshop"
version = "0.1.0"
description = "Template repo for the agentic-coding course — FastAPI + SQLite + vanilla JS smoke-test app."
requires-python = ">=3.13"
dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "sqlalchemy>=2.0",
    "pydantic>=2.0",
    "httpx",
]

[dependency-groups]
dev = [
    "pytest",
    "ruff",
    "respx",
]

[tool.uv]
package = false

[tool.uv.workspace]
members = [
    "strompris-mcp",
]

[tool.ruff]
target-version = "py313"

[tool.pytest.ini_options]
pythonpath = ["backend"]
